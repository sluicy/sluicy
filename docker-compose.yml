services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: sluicy
      POSTGRES_PASSWORD: sluicy
      POSTGRES_DB: sluicy
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sluicy"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
